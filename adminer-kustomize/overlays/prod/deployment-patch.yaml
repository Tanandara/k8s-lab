apiVersion: apps/v1
kind: Deployment
metadata:
  name: adminer-kustomize
spec:
  template:
    spec:
      containers:
        - name: adminer
          resources:
            requests:
              cpu: 250m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi