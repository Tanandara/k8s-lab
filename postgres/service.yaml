apiVersion: v1
kind: Service

metadata:
  name: postgres
  namespace: eks-lab

spec:
  selector:
    app: postgres

  ports:
    - port: 5432
      targetPort: 5432

  type: ClusterIP


# apiVersion: v1
# kind: Service
# metadata:
#   name: postgres
#   namespace: default
# spec:
#   selector:
#     app: postgres
#   ports:
#     - protocol: TCP
#       port: 5432
#       targetPort: 5432
#   type: ClusterIP
