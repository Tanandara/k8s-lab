apiVersion: v1
kind: Pod
metadata:
  name: rbac-test
  namespace: eks-lab

spec:
  serviceAccountName: pod-reader

  containers:
    - name: kubectl
      image: bitnami/kubectl:latest
      command:
        - sleep
        - "3600"

  restartPolicy: Never